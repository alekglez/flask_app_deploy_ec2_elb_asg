# A staging environment (stage) is a nearly exact replica of a
# production environment for software testing. Staging environments
# are made to test codes, builds, and updates to ensure quality
# under a production-like environment before application deployment.

from .base import *

# Define specific configurations for this environment...
